default:
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          goutte: ~
  suites:
    web:
      paths:    [ features/web ]
      contexts: [ WebContext ]
    cli:
      paths:    [ features/cli ]
      contexts: [ CliContext ]
